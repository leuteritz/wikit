{
  "name": "wikit",
  "version": "2.50.0",
  "private": true,
  "type": "module",
  "description": "Wikit - a tiny self-hosted personal wiki / knowledge base (Vue + NestJS + TypeORM + SQLite), light enough for a Raspberry Pi",
  "license": "MIT",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -k -n API,WEB -c blue,green \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --workspace backend run start:dev",
    "dev:web": "npm --workspace frontend run dev",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "start": "node backend/dist/main.js"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "multer": "^2.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
