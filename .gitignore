node_modules/
frontend/dist/
backend/dist/

# --- User data: NEVER commit personal content ---
# The actual database (your knowledge base) stays local.
backend/data/*.db
backend/data/*.db-*
# Your personal seed content. The committed demo lives in seed/articles.example/ + manifest.example.js
backend/seed/articles/
backend/seed/manifest.js

# Env / misc
.env
*.log
.DS_Store
Thumbs.db

CLAUDE.md
# .github/ MUSS getrackt werden — sonst landet der Deploy-Workflow nie auf GitHub
# und die CI/CD-Pipeline (Self-Hosted Runner) feuert nie.
